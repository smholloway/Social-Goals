<% if Rails.env.development? %>
$("#notices").html("app/views/tasks/update.js.erb");
<% end %>

/* Render the partial with all tasks */
$("#tasks_list").html("<%= escape_javascript( render(@goal.tasks ) ) %>");
